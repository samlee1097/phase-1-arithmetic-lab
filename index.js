const num1 = 2,
      num2 = 31,
      multiply = num1 * num2,
      random = Math.ceil(Math.random() * 10**(Math.ceil(Math.random() * 10))),
      num3 = 10,
      num4 = 6,
      mod = num3 % num4,
      max = Math.max(1,3,4,20,12,11);